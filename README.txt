Code is available at https://github.com/jacklbruck/markov_decision_processes.

First create an environment however you would like to.

Then pull and pip install this repository, preferably with the -e flag.

To get the results for all algorithms on all problems, run python -m markov_decision_processes.main.

The results will be written to a folder within the install location as npy files.